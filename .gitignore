# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# uv / venv
.venv/
.python-version.local

# Personal data (privacy-sensitive — never commit)
data/

# Pipeline outputs
outputs/*
!outputs/.gitkeep
# outputs/best_params.json — gitignored per Phase 5 D-11 (machine/dataset-specific)

# ClearML
.clearml/
clearml.conf.local

# Ruff / pytest / ty cache
.ruff_cache/
.pytest_cache/
.ty_cache/

# Claude Code worktrees
.claude/worktrees/
.pytest_durations.json
