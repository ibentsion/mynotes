---
phase: 07-augmentation-and-two-stage-training
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_train_ctc.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "pytest tests/test_train_ctc.py -q exits 0 with no failures"
  artifacts:
    - path: "tests/test_train_ctc.py"
      provides: "Fixed fake_build_charset mock signatures"
      contains: "extra_words=None"
  key_links:
    - from: "tests/test_train_ctc.py fake_build_charset"
      to: "src/ctc_utils.py::build_charset"
      via: "patch('src.ctc_utils.build_charset', side_effect=fake_build_charset)"
      pattern: "def fake_build_charset.*extra_words"
---

<objective>
Fix two pre-existing test failures in test_train_ctc.py before Phase 7 feature work begins.

Purpose: Establish a green baseline so Phase 7 failures are attributable to new code, not pre-existing issues.
Output: tests/test_train_ctc.py with corrected mock signatures; all existing tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix fake_build_charset mock signatures in test_train_ctc.py</name>
  <files>tests/test_train_ctc.py</files>
  <read_first>
    - tests/test_train_ctc.py (lines 110-157 and 244-291) — the two failing tests; read both before touching anything
    - src/ctc_utils.py (lines 18-33) — build_charset actual signature: (labels, extra_words=None)
  </read_first>
  <action>
    In tests/test_train_ctc.py, two local mock functions accept only one positional parameter but
    build_charset is called as build_charset(labels, extra_words=extra_words). The call fails with
    TypeError because the mocks do not accept the keyword argument.

    Fix both:

    1. In test_status_filter_keeps_only_labeled (around line 129):
       Change `def fake_build_charset(labels):` to `def fake_build_charset(labels, extra_words=None):`

    2. In test_charset_build_receives_labeled_labels (around line 264):
       Change `def capture_build_charset(labels):` to `def capture_build_charset(labels, extra_words=None):`

    No other changes. Do not modify test logic, assertions, or any other tests.
  </action>
  <verify>
    <automated>cd /home/ido/git/mynotes && uv run pytest tests/test_train_ctc.py::test_status_filter_keeps_only_labeled tests/test_train_ctc.py::test_charset_build_receives_labeled_labels -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    Both tests pass. No other test_train_ctc.py tests regress.
    Acceptance criteria:
    - tests/test_train_ctc.py line for fake_build_charset contains "extra_words=None"
    - tests/test_train_ctc.py line for capture_build_charset contains "extra_words=None"
    - `uv run pytest tests/test_train_ctc.py -q` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → production code | Unit tests invoke production via monkeypatching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-SC | Tampering | No new packages installed | accept | Wave 0 — test-only fix, no new deps |
</threat_model>

<verification>
Run full test_train_ctc.py suite to confirm zero failures:
`uv run pytest tests/test_train_ctc.py -q`

Also confirm ctc_utils tests still pass:
`uv run pytest tests/test_ctc_utils.py -q`
</verification>

<success_criteria>
- `uv run pytest tests/test_train_ctc.py -q` exits 0, no failures
- `uv run pytest tests/test_ctc_utils.py -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/07-augmentation-and-two-stage-training/07-01-SUMMARY.md` when done
</output>
