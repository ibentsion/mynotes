---
phase: 07-augmentation-and-two-stage-training
plan: "02"
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - pyproject.toml
  - src/ctc_utils.py
  - tests/test_ctc_utils.py
autonomous: true
requirements:
  - AUG-01
must_haves:
  truths:
    - "AugmentTransform accepts elastic_alpha and elastic_sigma constructor params"
    - "When elastic_alpha > 0, ElasticTransform + GridDistortion are applied after existing rotation/brightness/noise"
    - "When elastic_alpha == 0 (default), albumentations is never imported in the call path"
    - "Output tensor remains (1, H, W) float32 with values clamped to [0, 1]"
    - "albumentations==2.0.8 is a declared project dependency"
  artifacts:
    - path: "pyproject.toml"
      provides: "albumentations==2.0.8 dependency declaration"
      contains: "albumentations==2.0.8"
    - path: "src/ctc_utils.py"
      provides: "Extended AugmentTransform with elastic deformation"
      contains: "elastic_alpha"
    - path: "tests/test_ctc_utils.py"
      provides: "Tests for elastic augmentation behavior"
      contains: "test_augment_transform_elastic"
  key_links:
    - from: "AugmentTransform.__call__"
      to: "albumentations.ElasticTransform + albumentations.GridDistortion"
      via: "deferred import inside if elastic_alpha > 0 block"
      pattern: "import albumentations as A.*noqa.*PLC0415"
    - from: "AugmentTransform.__call__ tensor conversion"
      to: "numpy (H, W, 1)"
      via: "tensor.squeeze(0).numpy()[:, :, np.newaxis]"
      pattern: "np.newaxis"
---

<objective>
Add albumentations as a project dependency and extend AugmentTransform in ctc_utils.py with
elastic deformation capability (per D-01, D-02).

Purpose: Enable optional elastic deformation augmentation on training crops without breaking
the existing (1, H, W) tensor contract or adding import overhead when elastic_alpha=0.
Output: pyproject.toml updated; AugmentTransform gains elastic_alpha/elastic_sigma params;
test coverage for the new elastic path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-augmentation-and-two-stage-training/07-CONTEXT.md
@.planning/phases/07-augmentation-and-two-stage-training/07-RESEARCH.md
@.planning/phases/07-augmentation-and-two-stage-training/07-01-SUMMARY.md
</context>

<interfaces>
From src/ctc_utils.py — AugmentTransform current contract (read the file before editing):

AugmentTransform.__init__ current signature:
  def __init__(self, rotation_max=7.0, brightness_delta=0.10, noise_sigma=0.02) -> None

AugmentTransform.__call__ current signature:
  def __call__(self, tensor: torch.Tensor, seed: int | None = None) -> torch.Tensor
  Input:  (1, H, W) float32 tensor with values in [0, 1]
  Output: (1, H, W) float32 tensor with values in [0, 1]
  Existing steps: (1) rotation via affine_grid, (2) brightness jitter, (3) Gaussian noise

albumentations 2.x API (verified in RESEARCH.md):
  - fill= not value= for fill parameters
  - p=1.0 not always_apply=True
  - distort_range=(-0.15, 0.15) not distort_limit
  - Direct transform calls require (H, W, C) — always use A.Compose([...])
  - border_mode=0 means cv2.BORDER_CONSTANT
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add albumentations dep and extend AugmentTransform with elastic path</name>
  <files>pyproject.toml, src/ctc_utils.py</files>
  <read_first>
    - pyproject.toml — current dependencies section; read before adding albumentations
    - src/ctc_utils.py lines 156-208 — AugmentTransform __init__ and __call__ to extend
  </read_first>
  <behavior>
    - AugmentTransform(elastic_alpha=0.0) — elastic_alpha defaults to 0 (disabled, D-02)
    - AugmentTransform(elastic_sigma=5.0) — elastic_sigma defaults to 5.0 (midpoint of 4-6 range, D-02)
    - When elastic_alpha == 0: __call__ returns without any albumentations import or call
    - When elastic_alpha > 0: ElasticTransform + GridDistortion applied AFTER rotation/brightness/noise
    - Output shape is always (1, H, W) float32 after elastic; values clamped to [0, 1]
    - import albumentations is deferred inside the if block with # noqa: PLC0415
  </behavior>
  <action>
    Step 1 — Add albumentations to pyproject.toml:
    Insert `"albumentations==2.0.8",` into the dependencies list in pyproject.toml, after the
    existing torch entry. Then run `uv sync` to install it and update uv.lock.

    Step 2 — Extend AugmentTransform.__init__ in src/ctc_utils.py:
    Add two new params after noise_sigma:
      elastic_alpha: float = 0.0   # 0 = disabled (D-02)
      elastic_sigma: float = 5.0   # only used when elastic_alpha > 0
    Store both: self.elastic_alpha = elastic_alpha; self.elastic_sigma = elastic_sigma.

    Step 3 — Extend AugmentTransform.__call__ in src/ctc_utils.py:
    After the existing three steps (rotation, brightness, noise), append an elastic block
    guarded by `if self.elastic_alpha > 0:`.

    Inside the block:
    - Deferred import: `import albumentations as A  # noqa: PLC0415`
    - Also import numpy: `import numpy as np  # noqa: PLC0415` (if not already at module level)
    - Build transform with A.Compose([ElasticTransform, GridDistortion]):
        A.ElasticTransform(alpha=self.elastic_alpha, sigma=self.elastic_sigma,
                           border_mode=0, fill=0.0, p=1.0)
        A.GridDistortion(num_steps=5, distort_range=(-0.15, 0.15),
                         border_mode=0, fill=0.0, p=1.0)
    - Convert tensor to numpy: img_np = tensor.squeeze(0).numpy() gives (H, W) float32
    - Add channel dim: img_hwc = img_np[:, :, np.newaxis] gives (H, W, 1) — required by albu
    - Apply: result = transform(image=img_hwc)["image"] gives (H, W, 1) float32
    - Strip channel and convert back: tensor = torch.from_numpy(result[:, :, 0]).unsqueeze(0)
    - Clamp defensively: tensor = torch.clamp(tensor, 0.0, 1.0)

    Key constraints (per RESEARCH.md):
    - Use fill= not value= (albumentations 2.x breaking change)
    - Use p=1.0 not always_apply=True (removed in 2.x)
    - Use distort_range= not distort_limit= (renamed in 2.x)
    - Use A.Compose([...]) wrapper — direct calls require explicit (H, W, 1) but Compose is safer
    - border_mode=0 is cv2.BORDER_CONSTANT (consistent with existing padding pattern)
    - The deferred import matches the existing # noqa: PLC0415 pattern in the codebase
    - numpy may already be imported at module level; check before adding redundant import
  </action>
  <verify>
    <automated>cd /home/ido/git/mynotes && uv run python -c "from src.ctc_utils import AugmentTransform; import torch; t = torch.rand(1, 64, 128); a = AugmentTransform(elastic_alpha=30.0, elastic_sigma=5.0); out = a(t); assert out.shape == (1, 64, 128) and out.min() >= 0.0 and out.max() <= 1.0, f'failed: {out.shape} {out.min()} {out.max()}'; print('OK')"</automated>
  </verify>
  <done>
    - pyproject.toml contains "albumentations==2.0.8"
    - uv sync succeeds (uv.lock updated)
    - AugmentTransform(elastic_alpha=0.0, elastic_sigma=5.0) accepted without error
    - AugmentTransform with elastic_alpha=30.0 transforms (1, 64, 128) tensor → (1, 64, 128) tensor
    - Output values are in [0.0, 1.0]
    - AugmentTransform() with no args behaves identically to before (elastic_alpha=0 default)
    - `uv run ruff check src/ctc_utils.py` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add elastic augmentation tests to test_ctc_utils.py</name>
  <files>tests/test_ctc_utils.py</files>
  <read_first>
    - tests/test_ctc_utils.py — full file; understand existing test structure before appending
    - src/ctc_utils.py — AugmentTransform after Task 1 edits (confirms final interface)
  </read_first>
  <behavior>
    - Test: AugmentTransform() with no args has elastic_alpha=0.0 and elastic_sigma=5.0 attributes
    - Test: elastic_alpha=0 default does NOT import albumentations (no import side-effect)
    - Test: elastic_alpha=30.0 produces output tensor with same shape (1, H, W) as input
    - Test: elastic_alpha=30.0 output values are clamped to [0.0, 1.0]
    - Test: elastic_alpha=30.0 with seed produces different result than elastic_alpha=0 (content changes)
    - Test: existing AugmentTransform behavior is unaffected (regression guard — confirm default still works)
  </behavior>
  <action>
    Append a new test section to tests/test_ctc_utils.py titled
    "AugmentTransform — elastic deformation (Phase 7 AUG-01)".

    Test names to add:
    - test_augment_transform_elastic_defaults — AugmentTransform() has elastic_alpha=0.0, elastic_sigma=5.0
    - test_augment_transform_elastic_zero_skips_albumentations — elastic_alpha=0 means albumentations
      is not imported (mock sys.modules or just verify output is deterministically the same as pre-existing
      path with seed)
    - test_augment_transform_elastic_preserves_shape — with elastic_alpha=30.0, input (1, 64, 200)
      tensor produces output (1, 64, 200) tensor
    - test_augment_transform_elastic_values_clamped — output values in [0.0, 1.0] with elastic_alpha=30.0
    - test_augment_transform_elastic_modifies_content — with elastic_alpha=30.0 and seed=42, output
      tensor is NOT identical to the input (elastic deformation actually changed pixels); use
      `assert not torch.equal(output, input_tensor)` — use a non-trivial input (not all-zeros/all-ones)

    Use torch.rand(1, 64, 200) or torch.rand(1, 64, 128) as test tensors.
    Use seed=42 where determinism is needed. All tests should pass without requiring GPU.
  </action>
  <verify>
    <automated>cd /home/ido/git/mynotes && uv run pytest tests/test_ctc_utils.py -q -k "elastic" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - At least 5 new test functions matching test_augment_transform_elastic_* exist in tests/test_ctc_utils.py
    - All elastic tests pass
    - Full test_ctc_utils.py suite still passes: `uv run pytest tests/test_ctc_utils.py -q`
    - `uv run pytest tests/test_ctc_utils.py tests/test_train_ctc.py -q` exits 0 (no regressions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CropDataset.__getitem__ → AugmentTransform | Training crops flow through; elastic output must stay in [0,1] float32 |
| CLI args → AugmentTransform constructor | User-supplied alpha/sigma values need no validation at this layer (they're floats) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | albumentations install | mitigate | albumentations is a widely-known library (RESEARCH.md Package Legitimacy Audit: Approved); exact version pin ==2.0.8 |
| T-07-02-02 | Tampering | tensor values post-elastic | mitigate | torch.clamp(tensor, 0.0, 1.0) after conversion; albumentations auto-clips float32 internally but explicit clamp is defensive |
| T-07-02-SC | Tampering | npm/pip/cargo installs | mitigate | albumentations==2.0.8 tagged Approved in RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
```bash
cd /home/ido/git/mynotes
uv run pytest tests/test_ctc_utils.py tests/test_train_ctc.py -q
uv run ruff check src/ctc_utils.py
python -c "import albumentations; print(albumentations.__version__)"
```
All must exit 0 / print "2.0.8".
</verification>

<success_criteria>
- albumentations==2.0.8 in pyproject.toml and importable
- AugmentTransform accepts elastic_alpha and elastic_sigma; default elastic_alpha=0.0
- elastic_alpha=0 path: no albumentations import, tensor unchanged by elastic
- elastic_alpha=30.0 path: output is (1, H, W) float32 in [0, 1]
- ruff check src/ctc_utils.py exits 0
- All existing ctc_utils and train_ctc tests still pass
</success_criteria>

<output>
Create `.planning/phases/07-augmentation-and-two-stage-training/07-02-SUMMARY.md` when done
</output>
