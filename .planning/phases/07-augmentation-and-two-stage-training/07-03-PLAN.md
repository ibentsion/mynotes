---
phase: 07-augmentation-and-two-stage-training
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - src/train_ctc.py
  - src/tune.py
  - tests/test_train_ctc.py
autonomous: true
requirements:
  - AUG-02
must_haves:
  truths:
    - "--elastic_alpha and --elastic_sigma are accepted CLI flags in train_ctc.py"
    - "--elastic_alpha defaults to 0.0 (disabled by default, D-02)"
    - "When elastic_alpha > 0, AugmentTransform is constructed with elastic_alpha and elastic_sigma"
    - "Elastic applies to real fine-tuning train split ONLY — not val, not synthetic (D-03)"
    - "tune.py _objective Namespace includes elastic_alpha=0.0 and elastic_sigma=5.0 with safe defaults"
    - "tune.py _build_parser does NOT expose elastic as tunable — it stays fixed at 0 for HPO trials"
  artifacts:
    - path: "src/train_ctc.py"
      provides: "--elastic_alpha and --elastic_sigma CLI flags"
      contains: "elastic_alpha"
    - path: "src/tune.py"
      provides: "Updated _objective Namespace with elastic flag safe defaults"
      contains: "elastic_alpha=0.0"
    - path: "tests/test_train_ctc.py"
      provides: "Tests for elastic CLI flags and wiring"
      contains: "test_elastic"
  key_links:
    - from: "train_ctc.py _build_parser"
      to: "AugmentTransform(elastic_alpha=..., elastic_sigma=...)"
      via: "args.elastic_alpha, args.elastic_sigma passed to AugmentTransform constructor"
      pattern: "AugmentTransform.*elastic_alpha.*elastic_sigma"
    - from: "tune.py _objective"
      to: "argparse.Namespace"
      via: "elastic_alpha=0.0, elastic_sigma=5.0 in Namespace(...)"
      pattern: "elastic_alpha=0\\.0"
---

<objective>
Wire --elastic_alpha and --elastic_sigma CLI flags into train_ctc.py and update tune.py's
_objective Namespace to include elastic flag safe defaults (per AUG-02, D-02, D-03).

Purpose: Expose elastic augmentation strength via CLI; prevent AttributeError in HPO trials
by keeping tune.py Namespace in sync with train_ctc.py's new flags.
Output: train_ctc.py with elastic flags wired into AugmentTransform construction; tune.py
_objective Namespace updated; tests covering the new flags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-augmentation-and-two-stage-training/07-CONTEXT.md
@.planning/phases/07-augmentation-and-two-stage-training/07-RESEARCH.md
@.planning/phases/07-augmentation-and-two-stage-training/07-02-SUMMARY.md
</context>

<interfaces>
From src/train_ctc.py — current AugmentTransform construction (read the file before editing):

Current construction (around line 413):
  augment = AugmentTransform(
      rotation_max=args.rotation_max,
      brightness_delta=args.brightness_delta,
      noise_sigma=args.noise_sigma,
  )

Target construction (after this plan):
  augment = AugmentTransform(
      rotation_max=args.rotation_max,
      brightness_delta=args.brightness_delta,
      noise_sigma=args.noise_sigma,
      elastic_alpha=args.elastic_alpha,   # 0.0 = disabled (D-02)
      elastic_sigma=args.elastic_sigma,
  )

From src/tune.py — _objective Namespace construction (lines 135-159):
The Namespace must include elastic_alpha=0.0 and elastic_sigma=5.0 added alongside the
existing aug params. These values are safe defaults — HPO trials don't tune elastic params.

D-03 constraint: elastic applies to real fine-tuning train split ONLY. The AugmentTransform
is only created when aug_copies > 0 in run_training(); this is unchanged. The synthetic
pre-training path (when added in Plan 07-04) will NOT pass elastic params.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --elastic_alpha and --elastic_sigma CLI flags to train_ctc.py</name>
  <files>src/train_ctc.py</files>
  <read_first>
    - src/train_ctc.py lines 44-153 — _build_parser(); read to find insertion point for new flags
    - src/train_ctc.py lines 409-435 — AugmentTransform construction in run_training(); read to locate exact edit
    - src/ctc_utils.py lines 163-171 — AugmentTransform.__init__ new signature (from Plan 07-02); confirm exact param names
  </read_first>
  <behavior>
    - _build_parser() exposes --elastic_alpha with type=float, default=0.0
      help text: "Elastic deformation alpha (displacement magnitude). 0 = disabled (D-02). Try 30-80 for visible warping."
    - _build_parser() exposes --elastic_sigma with type=float, default=5.0
      help text: "Elastic deformation sigma (smoothness). Used only when --elastic_alpha > 0."
    - AugmentTransform construction passes elastic_alpha=args.elastic_alpha, elastic_sigma=args.elastic_sigma
    - The existing `if args.aug_copies > 0:` guard remains unchanged — elastic only applies when augmentation is active
  </behavior>
  <action>
    In src/train_ctc.py, make two edits:

    Edit 1 — _build_parser(): Add two new argument definitions after the existing noise_sigma argument
    (around line 79). Follow exact pattern of surrounding arguments:
      p.add_argument("--elastic_alpha", type=float, default=0.0,
          help="Elastic deformation alpha (displacement magnitude). 0 = disabled (D-02). Try 30-80.")
      p.add_argument("--elastic_sigma", type=float, default=5.0,
          help="Elastic deformation sigma (smoothness). Used only when --elastic_alpha > 0.")

    Edit 2 — run_training() AugmentTransform construction: The existing construction inside
    `if args.aug_copies > 0:` passes rotation_max, brightness_delta, noise_sigma. Add:
      elastic_alpha=args.elastic_alpha,
      elastic_sigma=args.elastic_sigma,
    after noise_sigma=args.noise_sigma.

    No other changes to run_training(). D-03: elastic is inside the aug_copies guard which
    already gates the real train split only. Val split is unaffected (no augment).
  </action>
  <verify>
    <automated>cd /home/ido/git/mynotes && uv run python -c "from src.train_ctc import _build_parser; args = _build_parser().parse_args(['--manifest','m.csv']); assert hasattr(args,'elastic_alpha'); assert args.elastic_alpha == 0.0; assert hasattr(args,'elastic_sigma'); assert args.elastic_sigma == 5.0; print('OK')"</automated>
  </verify>
  <done>
    - _build_parser() accepts --elastic_alpha (default 0.0) and --elastic_sigma (default 5.0)
    - AugmentTransform construction in run_training() includes elastic_alpha=args.elastic_alpha and elastic_sigma=args.elastic_sigma
    - `uv run ruff check src/train_ctc.py` exits 0
    - `uv run pytest tests/test_train_ctc.py -q` still exits 0 (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update tune.py _objective Namespace and add elastic + tune.py tests</name>
  <files>src/tune.py, tests/test_train_ctc.py</files>
  <read_first>
    - src/tune.py lines 127-171 — _objective() Namespace construction; read the full Namespace to understand what to add
    - src/train_ctc.py lines 44-153 — _build_parser() after Task 1 edits; every new flag here must appear in _objective Namespace
    - tests/test_train_ctc.py — read full file end to find correct test append point
  </read_first>
  <behavior>
    tune.py _objective Namespace (RESEARCH.md Pitfall 3):
    - elastic_alpha=0.0 — HPO trials don't tune elastic; safe default keeps it off
    - elastic_sigma=5.0 — matches _build_parser default

    tests/test_train_ctc.py new tests:
    - test_build_parser_elastic_defaults: _build_parser().parse_args(['--manifest','m.csv']) has
      elastic_alpha==0.0 and elastic_sigma==5.0
    - test_elastic_alpha_nonzero_wires_into_augment_transform: mock AugmentTransform constructor,
      call run_training with elastic_alpha=30.0 and aug_copies=1; verify AugmentTransform was
      constructed with elastic_alpha=30.0 (not 0.0); use minimal manifest fixture
    - test_objective_namespace_has_elastic_defaults: import _objective indirectly by verifying
      tune.py source contains "elastic_alpha=0.0" (grep assertion) — OR construct a mock trial
      and call _objective; simplest approach: inspect tune.py source text for presence of
      "elastic_alpha=0.0" and "elastic_sigma=5.0" in the _objective function
  </behavior>
  <action>
    Edit 1 — src/tune.py _objective(): Add elastic_alpha and elastic_sigma to the argparse.Namespace
    construction. Insert after noise_sigma=params["noise_sigma"]:
      elastic_alpha=0.0,
      elastic_sigma=5.0,

    These are NOT sourced from trial suggestions — HPO does not tune elastic params. They must
    be hardcoded as safe defaults so run_training() can access args.elastic_alpha without
    AttributeError (RESEARCH.md Pitfall 3).

    Edit 2 — tests/test_train_ctc.py: Append three new tests at the end of the file.

    test_build_parser_elastic_defaults:
      Calls _build_parser().parse_args(['--manifest','m.csv']); asserts elastic_alpha==0.0
      and elastic_sigma==5.0.

    test_elastic_alpha_nonzero_wires_into_augment_transform:
      Uses the existing _make_labeled_manifest helper. Patches src.ctc_utils.AugmentTransform
      to spy on constructor kwargs. Calls run_training with args including elastic_alpha=30.0
      and aug_copies=1. Asserts the spy was called with elastic_alpha=30.0.
      Requires @patch("src.train_ctc.Task") to provide Task.current_task() mock.

    test_tune_objective_namespace_has_elastic_attrs:
      Reads src/tune.py source text (Path("src/tune.py").read_text()). Asserts
      "elastic_alpha=0.0" appears in the source. Asserts "elastic_sigma=5.0" appears.
      This is a regression guard — if someone adds a new flag to train_ctc._build_parser
      and forgets tune._objective, this pattern will remind them.
  </action>
  <verify>
    <automated>cd /home/ido/git/mynotes && uv run pytest tests/test_train_ctc.py -q -k "elastic" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - tune.py _objective Namespace contains elastic_alpha=0.0 and elastic_sigma=5.0
    - Three new elastic tests in test_train_ctc.py pass
    - `uv run pytest tests/test_train_ctc.py -q` exits 0 (all tests pass including new ones)
    - `uv run ruff check src/tune.py` exits 0
    - `uv run pytest tests/test_tune.py -q` exits 0 (tune.py tests unaffected)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → AugmentTransform | User-supplied alpha value (float); no validation needed, 0.0 disables |
| tune.py _objective → run_training | Namespace must include all attrs accessed by run_training |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | tune.py Namespace out of sync | mitigate | Explicit test (test_tune_objective_namespace_has_elastic_attrs) asserts attributes present |
| T-07-03-SC | Tampering | No new packages in this plan | accept | No new deps; modification-only plan |
</threat_model>

<verification>
```bash
cd /home/ido/git/mynotes
uv run pytest tests/test_train_ctc.py tests/test_tune.py -q
uv run ruff check src/train_ctc.py src/tune.py
```
All must exit 0.
</verification>

<success_criteria>
- train_ctc._build_parser() accepts --elastic_alpha (default 0.0) and --elastic_sigma (default 5.0)
- run_training() passes elastic_alpha and elastic_sigma to AugmentTransform constructor
- tune.py _objective Namespace has elastic_alpha=0.0 and elastic_sigma=5.0
- All tests pass including three new elastic tests
</success_criteria>

<output>
Create `.planning/phases/07-augmentation-and-two-stage-training/07-03-SUMMARY.md` when done
</output>
