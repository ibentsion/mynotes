---
phase: 07-augmentation-and-two-stage-training
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - src/train_ctc.py
  - src/tune.py
  - tests/test_train_ctc.py
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
must_haves:
  truths:
    - "_run_loop() private helper contains the epoch loop; each of run_training, _run_pretrain, _run_loop is under 100 lines"
    - "run_training() with --pretrain_manifest set calls _run_pretrain() and returns WITHOUT fine-tuning"
    - "run_training() without --pretrain_manifest behaves identically to before (backward compat)"
    - "--pretrain_checkpoint_path loads model weights before fine-tuning"
    - "Pre-training val split is random (val_frac); fine-tuning val split uses build_half_page_units"
    - "Pre-training scalars logged with series prefix 'pretrain/' (e.g. title='loss', series='pretrain/train')"
    - "checkpoint_pretrain.pt saved and uploaded as ClearML artifact after pre-training"
    - "tune.py _objective Namespace includes all new flags with safe defaults; tune.py _build_parser accepts --pretrain_checkpoint_path"
    - "on_epoch_end callback is only called during fine-tuning; pre-training passes on_epoch_end=None to _run_loop"
  artifacts:
    - path: "src/train_ctc.py"
      provides: "_run_loop, _run_pretrain, updated run_training; new CLI flags"
      contains: "_run_loop"
    - path: "src/tune.py"
      provides: "Updated _build_parser with --pretrain_checkpoint_path; _objective with new flag defaults"
      contains: "pretrain_checkpoint_path"
    - path: "tests/test_train_ctc.py"
      provides: "Tests for pre-train mode, fine-tune mode, and backward compat"
      contains: "test_pretrain"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 7 success criteria reflecting two-call interface"
      contains: "two-call"
  key_links:
    - from: "run_training() pretrain_manifest check"
      to: "_run_pretrain()"
      via: "if args.pretrain_manifest is not None: return _run_pretrain(...)"
      pattern: "pretrain_manifest.*_run_pretrain"
    - from: "_run_pretrain"
      to: "_run_loop(series_prefix='pretrain/', on_epoch_end=None)"
      via: "_run_loop call with series_prefix argument"
      pattern: "_run_loop.*series_prefix.*pretrain"
    - from: "run_training fine-tune path"
      to: "_run_loop(series_prefix='', on_epoch_end=on_epoch_end)"
      via: "_run_loop call — backward compatible"
      pattern: "_run_loop.*on_epoch_end"
    - from: "_run_loop logger.report_scalar"
      to: "ClearML series names"
      via: "f'{series_prefix}train' and f'{series_prefix}val'"
      pattern: "series_prefix"
    - from: "_run_pretrain checkpoint save"
      to: "upload_file_artifact"
      via: "upload_file_artifact(task, 'checkpoint_pretrain', checkpoint_pretrain_path)"
      pattern: "checkpoint_pretrain"
---

<objective>
Refactor run_training() to extract _run_loop() and add _run_pretrain(), then wire new CLI
flags for two-stage training (--pretrain_manifest, --pretrain_epochs, --pretrain_lr,
--pretrain_checkpoint_path). Update tune.py for compatibility. Update ROADMAP.md Phase 7
success criteria to reflect the two-call interface.

Purpose: Enable synthetic pre-training as a separate CLI invocation before real-data
fine-tuning, while keeping the existing tune.py and fine-tuning call sites unmodified.
Output: Refactored train_ctc.py with _run_loop + _run_pretrain; tune.py with forwarded
pretrain_checkpoint_path; tests for both modes; updated ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-augmentation-and-two-stage-training/07-CONTEXT.md
@.planning/phases/07-augmentation-and-two-stage-training/07-RESEARCH.md
@.planning/phases/07-augmentation-and-two-stage-training/07-03-SUMMARY.md
</context>

<interfaces>
From src/train_ctc.py after Plan 07-03 (read the full file before editing):

run_training() currently: ~278 lines (lines 329-606)
  - Deferred imports (lines 345-364)
  - Dataset construction (lines 366-456)
  - Model + optimizer init (lines 458-473)
  - Epoch loop (lines 479-593)  ← this becomes _run_loop()
  - Best weight restore + artifact upload (lines 595-605)

Target _run_loop signature (per RESEARCH.md Pattern 2, D-04):
  def _run_loop(
      model: CRNN,
      train_loader: DataLoader,
      val_loader: DataLoader,
      val_df: pd.DataFrame,
      optimizer: torch.optim.Optimizer,
      scheduler: torch.optim.lr_scheduler.LRScheduler,
      ctc_loss: torch.nn.CTCLoss,
      ctc_loss_per_sample: torch.nn.CTCLoss,
      device: torch.device,
      args: argparse.Namespace,
      logger: Any,
      charset: list[str],
      checkpoint_path: Path,
      *,                            # keyword-only after position 13 (≤5 positional is CLAUDE.md rule)
      series_prefix: str = "",
      on_epoch_end: Callable[[int, float], None] | None = None,
  ) -> float:                       # returns best_val_cer

CRITICAL: The function has >5 params. CLAUDE.md says ≤5 positional params. Use * to make
params after position 5 keyword-only. Private function so no docstring needed.

Line count strategy — _run_loop will contain the epoch loop. If it approaches 100 lines
(CLAUDE.md hard limit), extract the val-loop body into a _run_val_batch() helper or similar
that is called per-batch inside the val loop. The existing report helpers
(_report_annotated_crop, _report_prob_heatmap, _report_saliency_panel) are already separate
functions — the debug block in the loop delegates to them and counts as a few lines.

New CLI flags in _build_parser():
  --pretrain_manifest: type=Path, default=None,
      help="CSV manifest for synthetic pre-training crops. If set, runs pre-training only and saves checkpoint_pretrain.pt."
  --pretrain_epochs: type=int, default=0,
      help="Number of pre-training epochs on synthetic data (0 = skip pre-training, D-05)."
  --pretrain_lr: type=float, default=1e-3,
      help="Learning rate for pre-training (independent of --lr used for fine-tuning, D-07)."
  --pretrain_checkpoint_path: type=Path, default=None,
      help="Path to checkpoint_pretrain.pt to load weights before fine-tuning (D-06)."

From src/tune.py (read the full file before editing):
  _build_parser(): add --pretrain_checkpoint_path (type=Path, default=None)
  _objective() Namespace: add the four new flags with safe defaults:
    pretrain_manifest=None,
    pretrain_epochs=0,
    pretrain_lr=1e-3,
    pretrain_checkpoint_path=sweep_args.pretrain_checkpoint_path,  # forwarded from sweep args

  Note: pretrain_checkpoint_path must come from sweep_args (not hardcoded None) because the
  HPO workflow uses pre-trained weights when the user passes --pretrain_checkpoint_path to tune-hpo.

From src/clearml_utils.py:
  upload_file_artifact(task, name, path) — reuse for checkpoint_pretrain upload (D-10)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract _run_loop and add _run_pretrain to train_ctc.py; add pretrain CLI flags</name>
  <files>src/train_ctc.py</files>
  <read_first>
    - src/train_ctc.py — read the FULL file; understand current run_training() structure before any edits
    - src/clearml_utils.py lines 21-22 — upload_file_artifact signature
    - src/ctc_utils.py lines 248-297 — build_half_page_units and split_units (NOT called for synthetic split)
  </read_first>
  <action>
    This is a complex refactor. Work in this order to minimize risk:

    STEP 1 — Add four new CLI flags to _build_parser() (after noise_sigma group, before rnn_hidden group):
      --pretrain_manifest (type=Path, default=None)
      --pretrain_epochs (type=int, default=0)
      --pretrain_lr (type=float, default=1e-3)
      --pretrain_checkpoint_path (type=Path, default=None)

    STEP 2 — Extract _run_loop() from run_training():
    Move the epoch loop body (currently `for epoch in range(args.epochs):` through early stop) into a new
    private function `_run_loop`. Place it BEFORE run_training() in the file.

    Signature (enforce keyword-only after position 5 with bare *):
      def _run_loop(
          model,
          train_loader,
          val_loader,
          val_df,
          optimizer,
          *,
          scheduler,
          ctc_loss,
          ctc_loss_per_sample,
          device,
          args,
          logger,
          charset,
          checkpoint_path,
          series_prefix="",
          on_epoch_end=None,
      ) -> float:

    The function body is the epoch loop verbatim, with these changes:
    - `for epoch in range(args.epochs):` → keep as-is (args.epochs controls loop count)
    - All logger.report_scalar calls: wrap title strings with series_prefix on the series arg:
        `series="train"` → `series=f"{series_prefix}train"`
        `series="val"` → `series=f"{series_prefix}val"`
    - The debug sample block (predict_single_with_probs + _report_annotated_crop + _report_prob_heatmap
      + _report_saliency_panel) should only run when debug_samples is non-empty; it already works that
      way (len(debug_samples) == 0 when _run_pretrain passes empty list).
    - checkpoint_path param controls where the checkpoint is saved (replaces hardcoded args.output_dir / "checkpoint.pt")
    - on_epoch_end is called only if not None: `if on_epoch_end is not None: on_epoch_end(epoch, val_cer)`
    - patience_left state lives inside _run_loop (local var)
    - best_val_cer restores best weights at END of loop (torch.load from checkpoint_path)
    - Returns best_val_cer: float

    LINE COUNT ENFORCEMENT: Count the lines in _run_loop after extraction. If it exceeds 100 lines,
    extract the per-sample val scoring loop into a private helper _eval_val_batch(log_probs,
    labels, target_lengths, charset, val_df, dataset_idx, ctc_loss_per_sample, device) → tuple
    that returns (val_loss_contribution, cer_total_contribution, cer_count_contribution,
    blank_frac_contribution, empty_count, per_sample_cer_list). This keeps _run_loop under 100 lines.

    STEP 3 — Add _run_pretrain() function (place BEFORE _run_loop in file, or AFTER — either works):
    Signature:
      def _run_pretrain(
          model,
          args,
          device,
          logger,
          task,
          charset,
      ) -> float:

    Body:
    - Import DataLoader from torch.utils.data (already done in run_training deferred block; _run_pretrain
      is called from run_training so these are available)
    - Read synthetic manifest: synth_df = pd.read_csv(args.pretrain_manifest)
    - Random val split (D-08, no build_half_page_units — synthetic has no page structure):
        import math
        n = len(synth_df)
        n_val = max(1, math.ceil(n * args.val_frac))
        train_df = synth_df.iloc[n_val:].reset_index(drop=True)
        val_df_pre = synth_df.iloc[:n_val].reset_index(drop=True)
    - Build datasets: CropDataset(train_df, charset) and CropDataset(val_df_pre, charset)
      NO augment for synthetic pre-training (D-03: elastic on real fine-tuning only)
    - DataLoader for train and val with args.batch_size, args.num_workers, crnn_collate
    - Fresh optimizer: torch.optim.AdamW(model.parameters(), lr=args.pretrain_lr, weight_decay=args.weight_decay)
    - Fresh scheduler: torch.optim.lr_scheduler.ReduceLROnPlateau(optimizer, mode="min", factor=0.5, patience=6, min_lr=1e-6)
    - CTC loss instances: CTCLoss(blank=0, zero_infinity=True, reduction="mean") and per_sample variant
    - Override args.epochs for the loop duration: use a local variable pretrain_epochs=args.pretrain_epochs
      instead of mutating args. Pass a modified namespace or just use args with a separate local epochs var.
      Cleanest: create a pretrain_args = argparse.Namespace(**vars(args)); pretrain_args.epochs = args.pretrain_epochs
    - checkpoint_pretrain_path = args.output_dir / "checkpoint_pretrain.pt"
    - Call _run_loop with:
        series_prefix="pretrain/",
        on_epoch_end=None,  # no Optuna pruner during pre-training (D-05)
        checkpoint_path=checkpoint_pretrain_path,
        debug_samples=[],  # no debug renders during synthetic pre-training (RESEARCH.md Open Question 1)
      Wait — val_df arg to _run_loop must be a DataFrame; pass val_df_pre here.
    - After _run_loop returns: upload checkpoint
        upload_file_artifact(task, "checkpoint_pretrain", checkpoint_pretrain_path)
    - Return best_pretrain_cer

    STEP 4 — Modify run_training():
    After the deferred imports and BEFORE the existing labeled/manifest reading, insert:
      if args.pretrain_manifest is not None:
          return _run_pretrain(model_placeholder, ...)
    Wait — model isn't built yet at that point. Rethink ordering:

    The correct ordering in run_training() after refactor:
    1. Deferred imports (unchanged)
    2. task = Task.current_task(); logger = task.get_logger()
    3. Early return for pre-training: check args.pretrain_manifest AFTER imports but BEFORE the
       labeled dataset / charset / page split work (which doesn't apply to synthetic data).
       But we need model built first for _run_pretrain... OR _run_pretrain builds its own model.

    RESOLUTION: _run_pretrain builds its own model internally (it receives charset from run_training
    after charset is built from the REAL manifest). But if pretrain_manifest is set, we don't have
    a real manifest yet...

    CORRECT ARCHITECTURE (per CONTEXT.md D-05 — pre-train is standalone):
    When --pretrain_manifest is set, run_training() must:
    a. Build charset FROM the synthetic manifest (not from real labeled data — there is no real manifest in pre-train mode)
    b. Build model, run pre-training, save checkpoint, return
    c. NOT read args.manifest or do any real-data processing

    So the flow for run_training with pretrain_manifest set:
    1. Deferred imports
    2. task + logger
    3. if args.pretrain_manifest is not None:
         - Read synthetic manifest: synth_df = pd.read_csv(args.pretrain_manifest)
         - Build charset from synthetic labels: charset = build_charset(synth_df["label"].tolist())
         - Save charset: save_charset(args.output_dir / "charset.json", charset)
         - Build model: CRNN(num_classes=len(charset)+1, rnn_hidden=args.rnn_hidden, num_layers=args.num_layers)
         - Set blank bias init: model.fc.bias.data[0] = args.blank_bias_init
         - Call _run_pretrain(model, args, device, logger, task, charset)
         - return result
    4. (else) existing fine-tune path...

    This means _run_pretrain doesn't need to build the model itself. Move model/device/charset
    setup into the if-pretrain block.

    For the fine-tune path, also add --pretrain_checkpoint_path weight loading (D-06):
    After model construction in the fine-tune path, before optimizer creation:
      if args.pretrain_checkpoint_path is not None:
          state = torch.load(args.pretrain_checkpoint_path, weights_only=True, map_location=device)
          model.load_state_dict(state)
          print(f"Loaded pre-trained weights from {args.pretrain_checkpoint_path}")

    STEP 5 — Replace the epoch loop in run_training() with a _run_loop call:
    The fine-tune path now calls:
      best_val_cer = _run_loop(
          model, train_loader, val_loader, val_df,
          optimizer,
          scheduler=scheduler,
          ctc_loss=ctc_loss,
          ctc_loss_per_sample=ctc_loss_per_sample,
          device=device,
          args=args,
          logger=logger,
          charset=charset,
          checkpoint_path=checkpoint_path,
          series_prefix="",
          on_epoch_end=on_epoch_end,
      )
    Remove the old epoch loop, best weight restore, and artifact upload from run_training
    (they move into _run_loop). Keep the artifact uploads for checkpoint.pt and charset.json
    AFTER _run_loop returns (since _run_loop saves checkpoint; run_training uploads it).

    STEP 6 — Verify line counts:
    Count lines in each function: run_training, _run_pretrain, _run_loop. All must be ≤100.
    If _run_loop is too long, extract _eval_val_epoch() helper as described above.
  </action>
  <verify>
    <automated>cd /home/ido/git/mynotes && uv run python -c "
import ast, pathlib
src = pathlib.Path('src/train_ctc.py').read_text()
tree = ast.parse(src)
for node in ast.walk(tree):
    if isinstance(node, ast.FunctionDef) and node.name in ('run_training', '_run_loop', '_run_pretrain'):
        end = node.end_lineno or 0
        lines = end - node.lineno + 1
        status = 'OK' if lines <= 100 else 'OVER LIMIT'
        print(f'{node.name}: {lines} lines [{status}]')
"</automated>
  </verify>
  <done>
    - _run_loop() exists in src/train_ctc.py
    - _run_pretrain() exists in src/train_ctc.py
    - run_training(), _run_loop(), _run_pretrain() are each ≤100 lines (verified by AST line count)
    - _build_parser() accepts --pretrain_manifest, --pretrain_epochs, --pretrain_lr, --pretrain_checkpoint_path
    - run_training() with pretrain_manifest set returns after pre-training (does NOT proceed to fine-tune path)
    - run_training() without pretrain_manifest behaves identically to before (backward compat)
    - _run_loop passes series_prefix to all report_scalar calls
    - `uv run ruff check src/train_ctc.py` exits 0
    - `uv run pytest tests/test_train_ctc.py -q` exits 0 (all existing tests pass)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update tune.py for pretrain_checkpoint_path and add two-stage tests + ROADMAP update</name>
  <files>src/tune.py, tests/test_train_ctc.py, .planning/ROADMAP.md</files>
  <read_first>
    - src/tune.py — read the full file; focus on _build_parser() and _objective() Namespace
    - src/train_ctc.py after Task 1 — confirm all new flags added; check _run_pretrain + _run_loop are present
    - tests/test_train_ctc.py — read to end to find append point for new tests
    - .planning/ROADMAP.md lines 82-90 — Phase 7 success criteria to update
  </read_first>
  <behavior>
    tune.py _build_parser():
    - Add --pretrain_checkpoint_path (type=Path, default=None)
      help="Path to pre-trained checkpoint; forwarded to each trial for fine-tuning from shared weights."

    tune.py _objective Namespace — add all four new train_ctc flags with safe defaults:
    - pretrain_manifest=None
    - pretrain_epochs=0
    - pretrain_lr=1e-3
    - pretrain_checkpoint_path=sweep_args.pretrain_checkpoint_path  (forwarded, not hardcoded None)

    tests/test_train_ctc.py — new tests:
    - test_build_parser_pretrain_flags_defaults: parse ['--manifest','m.csv']; assert
      pretrain_manifest is None, pretrain_epochs==0, pretrain_lr approx 1e-3,
      pretrain_checkpoint_path is None
    - test_pretrain_mode_runs_without_real_manifest: call run_training with args that have
      pretrain_manifest set to a synthetic CSV (use tmp_path); verify it returns a float and
      creates checkpoint_pretrain.pt in output_dir. Use minimal synthetic manifest with
      ~4 crops across 2 "labels". Requires @patch("src.train_ctc.Task") mock.
    - test_pretrain_mode_does_not_proceed_to_finetune: same setup as above; additionally verify
      build_half_page_units is NOT called (patch it to raise; if it doesn't raise, test passes).
    - test_finetune_loads_pretrain_checkpoint_path: create a fake checkpoint file (torch.save of
      empty state dict or CRNN state); run training with pretrain_checkpoint_path pointing to it;
      verify training completes (rc=0 via run_training return) without error. Use _make_labeled_manifest.
    - test_run_training_on_epoch_end_still_fires_in_finetune: confirm backward compat — on_epoch_end
      is still called during fine-tuning (no regression from _run_loop refactor). Reuse existing
      test_run_training_invokes_on_epoch_end_per_epoch pattern with 1 epoch.

    ROADMAP.md — update Phase 7 success criteria:
    Success criterion 2 currently says:
      "Running `train_ctc.py --pretrain_manifest synthetic.csv --pretrain_epochs 10` executes
      a synthetic pre-training loop before the real-data fine-tuning loop"
    Update to reflect two-call interface:
      "Running `train_ctc.py --pretrain_manifest synthetic.csv --pretrain_epochs 30` executes
      a standalone synthetic pre-training loop and saves `checkpoint_pretrain.pt` (no fine-tuning
      in the same invocation); fine-tuning is a separate call: `train_ctc.py --manifest real.csv
      --pretrain_checkpoint_path outputs/checkpoint_pretrain.pt`"
  </behavior>
  <action>
    Edit 1 — src/tune.py _build_parser(): Add one new argument after the --output_dir argument:
      p.add_argument("--pretrain_checkpoint_path", type=Path, default=None,
          help="Path to pre-trained checkpoint; forwarded to each HPO trial.")

    Edit 2 — src/tune.py _objective() Namespace: Add four new key-value pairs after noise_sigma
    (and after elastic_alpha/elastic_sigma from Plan 07-03):
      pretrain_manifest=None,
      pretrain_epochs=0,
      pretrain_lr=1e-3,
      pretrain_checkpoint_path=sweep_args.pretrain_checkpoint_path,

    Edit 3 — tests/test_train_ctc.py: Append five new tests (see behavior section above).
    For test_pretrain_mode_runs_without_real_manifest: construct a minimal synthetic manifest
    CSV with at least 4 rows: columns crop_path, label, status="labeled". Create tiny grayscale
    images for each crop. Use 4 labeled rows (2 train + 1 val after val_frac=0.5 or use 4 rows
    with val_frac=0.2 → 1 val, 3 train). Pass --pretrain_epochs=1, --batch_size=2, --rnn_hidden=128,
    --num_layers=1 to keep it fast. Verify: out_dir/checkpoint_pretrain.pt exists; return value is float.

    Edit 4 — .planning/ROADMAP.md: Find Phase 7 success criteria block (around lines 82-90).
    Update criterion 2 to reflect the two-call interface as described in the behavior section.
    Add a note that the ROADMAP success criterion was updated per CONTEXT.md user decision
    (two-call approach replaces single-invocation approach).
  </action>
  <verify>
    <automated>cd /home/ido/git/mynotes && uv run pytest tests/test_train_ctc.py -q -k "pretrain" 2>&1 | tail -15</automated>
  </verify>
  <done>
    - tune.py _build_parser() accepts --pretrain_checkpoint_path
    - tune.py _objective Namespace has pretrain_manifest=None, pretrain_epochs=0, pretrain_lr=1e-3,
      pretrain_checkpoint_path=sweep_args.pretrain_checkpoint_path
    - Five new pretrain tests pass in tests/test_train_ctc.py
    - All existing test_train_ctc.py tests still pass
    - ROADMAP.md Phase 7 success criterion 2 reflects the two-call interface
    - `uv run ruff check src/tune.py` exits 0
    - `uv run pytest tests/test_train_ctc.py tests/test_tune.py -q` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --pretrain_manifest file path | User-supplied CSV path; validated by pandas read_csv (raises FileNotFoundError on missing) |
| --pretrain_checkpoint_path | User-supplied checkpoint path; torch.load with weights_only=True prevents pickle RCE |
| tune.py _objective → run_training | Namespace must contain all four new flag attrs to avoid AttributeError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | --pretrain_checkpoint_path loading | mitigate | torch.load(..., weights_only=True) — established pattern; prevents arbitrary code execution via pickle |
| T-07-04-02 | Tampering | synthetic manifest path traversal | accept | Paths are user-supplied on trusted local machine; no server context |
| T-07-04-03 | Information Disclosure | pre-training on private synthetic data | accept | Data stays local; no external upload except ClearML (user-controlled) |
| T-07-04-04 | Denial of Service | _run_loop line count over 100 | mitigate | AST-based line count check in verify block; extract _eval_val_epoch if needed |
| T-07-04-SC | Tampering | No new packages in this plan | accept | Modification-only plan; no new deps |
</threat_model>

<verification>
Run the full Phase 7 test suite after both tasks complete:
```bash
cd /home/ido/git/mynotes
uv run pytest tests/test_train_ctc.py tests/test_ctc_utils.py tests/test_tune.py -q
uv run ruff check src/train_ctc.py src/tune.py
uv run python -c "
import ast, pathlib
src = pathlib.Path('src/train_ctc.py').read_text()
tree = ast.parse(src)
for node in ast.walk(tree):
    if isinstance(node, ast.FunctionDef) and node.name in ('run_training', '_run_loop', '_run_pretrain'):
        end = node.end_lineno or 0
        lines = end - node.lineno + 1
        assert lines <= 100, f'{node.name}: {lines} lines OVER LIMIT'
        print(f'{node.name}: {lines} lines OK')
"
```
All must pass / exit 0.
</verification>

<success_criteria>
- run_training(), _run_loop(), _run_pretrain() each ≤100 lines (AST verified)
- Pre-train mode: --pretrain_manifest triggers pre-training only; checkpoint_pretrain.pt created; returns without fine-tuning
- Fine-tune mode: --pretrain_checkpoint_path loads weights before training; uses build_half_page_units split
- on_epoch_end is called during fine-tuning but NOT during pre-training
- tune.py _build_parser accepts --pretrain_checkpoint_path; _objective Namespace has all four new flags
- ROADMAP.md Phase 7 criterion 2 reflects two-call interface
- All tests pass: `uv run pytest tests/test_train_ctc.py tests/test_tune.py -q`
</success_criteria>

<output>
Create `.planning/phases/07-augmentation-and-two-stage-training/07-04-SUMMARY.md` when done
</output>
