---
phase: 01-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .gitignore
  - uv.lock
  - src/__init__.py
  - tests/__init__.py
  - data/.gitkeep
  - outputs/.gitkeep
autonomous: true
requirements:
  - CLML-04
must_haves:
  truths:
    - "Running `uv run python --version` reports Python 3.13.x"
    - "Running `uv run python -c 'import cv2, pdf2image, clearml, numpy, pandas'` exits 0"
    - "Private data directories (data/pdfs, data/pages, data/crops, outputs) exist and are gitignored"
    - "pyproject.toml pins Python 3.13 and lists pinned versions for all five core dependencies"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, Python 3.13 pin, and pinned runtime deps"
      contains: "requires-python = \">=3.13\""
    - path: ".python-version"
      provides: "uv/pyenv Python version pin"
      contains: "3.13"
    - path: ".gitignore"
      provides: "Excludes venv and private data from git"
      contains: ".venv"
    - path: "uv.lock"
      provides: "Lockfile for reproducible installs"
      min_lines: 10
    - path: "src/__init__.py"
      provides: "src package marker"
      min_lines: 0
    - path: "tests/__init__.py"
      provides: "tests package marker"
      min_lines: 0
  key_links:
    - from: "pyproject.toml"
      to: ".venv via uv"
      via: "uv sync"
      pattern: "dependencies\\s*=\\s*\\["
    - from: ".gitignore"
      to: "data/, outputs/, .venv/"
      via: "git ignore rules"
      pattern: "^data/|^outputs/|\\.venv"
---

<objective>
Create the project scaffolding that every subsequent plan depends on: a uv-managed Python 3.13
virtualenv with the five pinned runtime dependencies (pdf2image, opencv-python-headless, numpy,
pandas, clearml), the src/ and tests/ package structure, the data/ and outputs/ directories used
by the pipeline, and a .gitignore that keeps the virtualenv and personal PDF data out of version
control.

Purpose: Every other Phase 1 plan imports from src/ and writes into data/ or outputs/. Without
the venv and layout, nothing can run. Python 3.13 is not yet installed on this machine (only 3.12
is present), so uv must fetch it before the venv is created.

Output: A working `.venv`, a committed pyproject.toml + uv.lock + .python-version + .gitignore,
and empty src/tests packages ready for code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline/01-RESEARCH.md

<interfaces>
<!-- No existing code to read — this plan creates the scaffolding. -->
<!-- Pinned dependency versions (from 01-RESEARCH.md Standard Stack section): -->

- pdf2image==1.17.0
- opencv-python-headless==4.13.0.92
- numpy==2.4.4
- pandas==3.0.2
- clearml==2.1.5

<!-- Dev/test pins (from CLAUDE.md global standards): ruff, ty, pytest -->

- ruff (latest)
- ty (latest)
- pytest (latest)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Python 3.13 and create uv project with pinned deps</name>
  <files>pyproject.toml, .python-version, uv.lock</files>
  <read_first>
    - .planning/phases/01-data-pipeline/01-RESEARCH.md (Environment Availability section — Python 3.13 needs `uv python install 3.13` first)
    - ~/.claude/CLAUDE.md (Python section — uv, ruff, ty, pytest are mandatory)
  </read_first>
  <action>
    1. Run `uv python install 3.13` to download Python 3.13 (not installed on this machine — only 3.12 is present).
    2. Create `pyproject.toml` in the repo root using `uv init --bare --python 3.13` then overwrite with this exact content:

    ```toml
    [project]
    name = "mynotes"
    version = "0.1.0"
    description = "Hebrew handwriting OCR pipeline — PDF to labeled dataset"
    requires-python = ">=3.13"
    dependencies = [
        "pdf2image==1.17.0",
        "opencv-python-headless==4.13.0.92",
        "numpy==2.4.4",
        "pandas==3.0.2",
        "clearml==2.1.5",
    ]

    [dependency-groups]
    dev = [
        "pytest>=8.0",
        "ruff>=0.6",
        "ty>=0.0.1a1",
    ]

    [build-system]
    requires = ["uv_build>=0.11"]
    build-backend = "uv_build"

    [tool.uv]
    package = true

    [tool.ruff]
    line-length = 100
    target-version = "py313"

    [tool.ruff.lint]
    select = ["E", "F", "I", "B", "UP", "SIM"]

    [tool.pytest.ini_options]
    testpaths = ["tests"]
    ```

    3. Create `.python-version` containing exactly the single line `3.13`.
    4. Run `uv sync` to create `.venv/` and write `uv.lock`. Verify `.venv/bin/python --version` prints `Python 3.13.x`.
    5. If `uv sync` fails because of a missing version for any pinned package on PyPI, stop and report the exact version mismatch — do NOT silently bump versions. The user confirmed these versions in 01-RESEARCH.md.
  </action>
  <verify>
    <automated>uv run python --version | grep -q "Python 3.13" && uv run python -c "import pdf2image, cv2, numpy, pandas, clearml; print('ok')" | grep -q "^ok$"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python --version` output matches regex `^Python 3\.13\.`
    - `grep -c 'requires-python = ">=3.13"' pyproject.toml` returns 1
    - `grep -cE 'pdf2image==1\.17\.0|opencv-python-headless==4\.13\.0\.92|numpy==2\.4\.4|pandas==3\.0\.2|clearml==2\.1\.5' pyproject.toml` returns 5
    - File `.python-version` contains exactly `3.13` on a single line
    - File `uv.lock` exists and contains the string `name = "pdf2image"`
    - `uv run python -c "import pdf2image, cv2, numpy, pandas, clearml"` exits with status 0
  </acceptance_criteria>
  <done>Python 3.13 venv exists under .venv/, all five runtime imports succeed, pyproject.toml and uv.lock are committed.</done>
</task>

<task type="auto">
  <name>Task 2: Create project directory layout and .gitignore</name>
  <files>src/__init__.py, tests/__init__.py, data/.gitkeep, outputs/.gitkeep, .gitignore</files>
  <read_first>
    - .planning/phases/01-data-pipeline/01-RESEARCH.md (Architecture Patterns → Recommended Project Structure — defines src/, data/pdfs/, data/pages/, data/crops/, outputs/)
    - .planning/PROJECT.md (Constraints → "Personal Hebrew notes only; privacy-sensitive — stays local" — data/ must be gitignored)
  </read_first>
  <action>
    Create the directory structure exactly as specified in 01-RESEARCH.md "Recommended Project Structure":

    1. Create empty files (use `mkdir -p` then `touch`):
       - `src/__init__.py` (empty)
       - `tests/__init__.py` (empty)
       - `data/.gitkeep` (empty)
       - `outputs/.gitkeep` (empty)

    2. Also create these subdirectories so pipeline output locations exist:
       - `data/pdfs/.gitkeep`
       - `data/pages/.gitkeep`
       - `data/crops/.gitkeep`

    3. Create `.gitignore` with EXACTLY this content:

    ```
    # Python
    __pycache__/
    *.py[cod]
    *$py.class
    *.so
    .Python

    # uv / venv
    .venv/
    .python-version.local

    # Personal data (privacy-sensitive — never commit)
    data/pdfs/*
    data/pages/*
    data/crops/*
    !data/pdfs/.gitkeep
    !data/pages/.gitkeep
    !data/crops/.gitkeep

    # Pipeline outputs
    outputs/*
    !outputs/.gitkeep

    # ClearML
    .clearml/
    clearml.conf.local

    # Ruff / pytest / ty cache
    .ruff_cache/
    .pytest_cache/
    .ty_cache/
    ```

    4. Do NOT gitignore `.python-version` itself — it must be committed for tool pinning. Only
       `.python-version.local` (optional override) is ignored.
  </action>
  <verify>
    <automated>test -f src/__init__.py && test -f tests/__init__.py && test -f data/pdfs/.gitkeep && test -f data/pages/.gitkeep && test -f data/crops/.gitkeep && test -f outputs/.gitkeep && grep -q "^data/pdfs/\*" .gitignore && grep -q "^\.venv/" .gitignore && grep -q "^outputs/\*" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `src/__init__.py`, `tests/__init__.py`
    - Directories exist with .gitkeep sentinels: `data/pdfs/.gitkeep`, `data/pages/.gitkeep`, `data/crops/.gitkeep`, `outputs/.gitkeep`
    - `.gitignore` contains literal line `.venv/`
    - `.gitignore` contains literal line `data/pdfs/*`
    - `.gitignore` contains literal line `data/pages/*`
    - `.gitignore` contains literal line `data/crops/*`
    - `.gitignore` contains literal line `outputs/*`
    - `.gitignore` contains literal line `!data/pdfs/.gitkeep`
    - `.python-version` is NOT listed in .gitignore (checked by: `grep -E "^\.python-version$" .gitignore` returns no matches)
    - Running `git check-ignore data/pdfs/secret.pdf` exits 0 (file is ignored)
    - Running `git check-ignore src/__init__.py` exits 1 (file is NOT ignored)
  </acceptance_criteria>
  <done>Project layout matches research doc; personal data directories exist but their contents are gitignored; src/ and tests/ are Python packages.</done>
</task>

</tasks>

<verification>
- `uv run python --version` → `Python 3.13.x`
- `uv run python -c "import pdf2image, cv2, numpy, pandas, clearml"` exits 0
- `git status` shows tracked `.gitignore`, `.python-version`, `pyproject.toml`, `uv.lock`, `src/__init__.py`, `tests/__init__.py`, and the four `.gitkeep` files
- `git check-ignore data/pdfs/anything.pdf` exits 0
- `.venv/` is NOT tracked by git (`git ls-files .venv | wc -l` returns 0)
</verification>

<success_criteria>
- Python 3.13 venv installed and working
- All five runtime dependencies import cleanly
- Project directory layout matches 01-RESEARCH.md spec
- Personal data paths gitignored but directories exist via .gitkeep
- pyproject.toml + uv.lock committed for reproducible installs
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-01-SUMMARY.md`.
</output>
