---
phase: 06-synthetic-generation
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/generate_synthetic.py
  - tests/test_generate_synthetic.py
autonomous: true
requirements: [SYN-03, SYN-04]
must_haves:
  truths:
    - "Corpus is built from existing labeled words, NFC-normalized, deduplicated, non-Hebrew-only words filtered out"
    - "Words containing rarer characters receive proportionally higher sampling weight"
    - "Text length per crop is sampled from the empirical character-count distribution of real labels"
    - "Coverage validation returns the set of characters below --min_char_count"
  artifacts:
    - path: "src/generate_synthetic.py"
      provides: "build_word_corpus, sample_text, build_char_count_distribution, check_coverage pure functions"
      contains: "def build_word_corpus"
    - path: "tests/test_generate_synthetic.py"
      provides: "Unit tests for corpus + coverage logic"
      contains: "def test_"
  key_links:
    - from: "build_word_corpus"
      to: "char frequency Counter"
      via: "inverse-frequency weight per word"
      pattern: "Counter"
    - from: "check_coverage"
      to: "min_char_count threshold"
      via: "Counter comparison returning sub-threshold chars"
      pattern: "min_char_count"
---

<objective>
Implement and unit-test the pure-Python core of synthetic generation: word corpus assembly
with inverse-frequency weighting (SYN-03), empirical text-length sampling (D-05), and
coverage validation (SYN-04). No image rendering, no ClearML, no file I/O here — these are
deterministic functions with clear input/output contracts, ideal for TDD.

Purpose: The hard correctness risks of this phase (rare-char weighting math, NFC handling,
Hebrew-only filtering, coverage gap detection) live in pure logic. Isolating and testing it
first means Plan 03's rendering loop wires verified primitives instead of debugging math and
pixels simultaneously.

Output: `src/generate_synthetic.py` containing four tested functions plus the module-level
imports/skeleton that Plan 03 extends with rendering + `main()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-synthetic-generation/06-RESEARCH.md
@.planning/phases/06-synthetic-generation/06-PATTERNS.md

<interfaces>
NFC normalization pattern (from src/ctc_utils.py build_charset, lines 18-28):
`unicodedata.normalize("NFC", text)` is applied to every label before any
character-level operation. Replicate this exactly.

Module-level import convention (every CLI script in src/, e.g. src/train_ctc.py:11):
`from clearml import Task  # noqa: F401  # module-level for test patchability`
This import is present even though Task is not used at module scope, so tests can
later patch `src.generate_synthetic.Task` (Plan 03 uses it; declare it now so the
import block is final).

Hebrew Unicode block for the Hebrew-only word filter (RESEARCH.md Pitfall 5):
letters are U+05D0–U+05EA. A word qualifies for the corpus if it contains at least
one character in that range.

Test file conventions: tests live in tests/, named test_<module>.py, plain pytest
functions named test_<behavior>, no class wrappers (see tests/test_ctc_utils.py,
tests/test_train_ctc.py for the established style). Run with `uv run pytest`.
</interfaces>

<reference_patterns>
RESEARCH.md Pattern 3 (build_word_corpus / sample_text) and Pattern 4
(build_char_count_distribution) give reference formulas. Treat the inverse-frequency
formula as the spec: word_score = sum over chars c in word of
`1.0 / (char_freq[c]/total_chars + 1e-8)`; weights = word_scores / word_scores.sum().
RESEARCH.md "Coverage Validation" code block gives the check_coverage contract.
Assumption A1 (weight formula) is the accepted spec for this plan — do not redesign it.
</reference_patterns>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — corpus builder, text sampler, char-count distribution</name>
  <files>src/generate_synthetic.py, tests/test_generate_synthetic.py</files>
  <read_first>
    - .planning/phases/06-synthetic-generation/06-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 5, Assumption A1, Open Question 2)
    - .planning/phases/06-synthetic-generation/06-PATTERNS.md (Imports pattern, NFC normalization pattern)
    - src/ctc_utils.py lines 18-40 (build_charset NFC normalization — replicate exactly)
    - tests/test_ctc_utils.py (pytest function style to mirror)
  </read_first>
  <behavior>
    - test_build_word_corpus_extracts_and_dedups_nfc_words: given labels
      `["שלום עולם", "שלום"]`, returns words containing `"שלום"` and `"עולם"` with no
      duplicate `"שלום"`; weights array length equals number of unique words and
      `np.isclose(weights.sum(), 1.0)`.
    - test_build_word_corpus_filters_non_hebrew_words: labels
      `["שלום 123 hello עולם"]` produce a word pool containing `"שלום"` and `"עולם"`
      but NOT `"123"` or `"hello"` (Hebrew-only filter, U+05D0–U+05EA, Pitfall 5).
    - test_build_word_corpus_rare_char_gets_higher_weight: construct labels where one
      char appears once and another many times; assert the weight of the word holding
      the rare char is strictly greater than the weight of a same-length word holding
      only common chars.
    - test_build_word_corpus_merges_extra_words: `extra_words=["נדיר"]` not present in
      labels appears in the returned word pool (NFC-normalized).
    - test_build_word_corpus_empty_labels_raises: empty label list raises `ValueError`
      with a message mentioning "no labeled" / "corpus".
    - test_sample_text_reaches_target_length_and_is_rtl_safe: with a fixed
      `np.random.default_rng(0)`, `sample_text` returns a space-joined string whose
      length is >= the target_chars argument, drawn only from the word pool, with no
      reversed/BiDi mangling (the returned string equals joined original words).
    - test_build_char_count_distribution_returns_nfc_lengths: labels
      `["אב", "אבג"]` → array equal to `[2, 3]` (NFC char counts).
  </behavior>
  <action>
    Create `src/generate_synthetic.py` with the final import block per
    06-PATTERNS.md Imports pattern: module docstring, `argparse`, `sys`,
    `unicodedata`, `pathlib.Path`, `numpy as np`, `pandas as pd`,
    `from collections import Counter`,
    `from clearml import Task  # noqa: F401  # module-level for test patchability`,
    `from src.clearml_utils import init_task, upload_file_artifact`. Do NOT import
    trdg/PIL at module level (Plan 03 imports trdg inside a function).

    Implement four pure functions with these exact signatures:
    - `build_word_corpus(labels: list[str], extra_words: list[str] | None = None) -> tuple[list[str], np.ndarray]`
      NFC-normalize every label; whitespace-split into words; keep a word only if it
      contains >= 1 char in U+05D0–U+05EA (Hebrew-only filter); include single-char
      Hebrew words (Open Question 2 recommendation — prepositions ב/ל/מ are valid);
      merge NFC-normalized `extra_words`; deduplicate (stable order). Compute char
      frequency with `Counter` over all normalized label chars; per-word score =
      `sum(1.0 / (char_freq[c]/total_chars + 1e-8) for c in word)`; return
      `(words, word_scores / word_scores.sum())`. Raise `ValueError` if no Hebrew
      words remain.
    - `sample_text(words: list[str], weights: np.ndarray, target_chars: int, rng: np.random.Generator) -> str`
      Repeatedly `rng.choice(words, p=weights)` accumulating `len(word)+1` until
      total >= target_chars; return `" ".join(selected)`. Always selects >= 1 word
      (guards Pitfall 4 narrow-image risk via minimum length).
    - `build_char_count_distribution(labels: list[str]) -> np.ndarray`
      Return `np.array([len(unicodedata.normalize("NFC", l)) for l in labels])`.

    Create `tests/test_generate_synthetic.py` with the behavior tests above first,
    confirm they FAIL (functions absent / NotImplementedError), then implement until
    they pass. Keep every function <= 100 lines, <= 5 positional params (CLAUDE.md
    hard limits). Run `uv run ruff check src/generate_synthetic.py` and
    `uv run ty check src/generate_synthetic.py` — zero warnings.
  </action>
  <verify>
    <automated>uv run pytest tests/test_generate_synthetic.py -x -q && uv run ruff check src/generate_synthetic.py tests/test_generate_synthetic.py && uv run ty check src/generate_synthetic.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_generate_synthetic.py` contains the 7 named test functions from
      `<behavior>`; `uv run pytest tests/test_generate_synthetic.py` exits 0
    - `git log --oneline -3` shows a `test(06-02):` commit preceding a `feat(06-02):`
      commit (RED then GREEN)
    - `src/generate_synthetic.py` contains `def build_word_corpus(`,
      `def sample_text(`, `def build_char_count_distribution(`
    - `src/generate_synthetic.py` contains the literal line
      `from clearml import Task  # noqa: F401  # module-level for test patchability`
    - `src/generate_synthetic.py` has NO module-level `import trdg` / `from trdg`
      / `from PIL` (grep returns nothing)
    - `uv run ruff check src/generate_synthetic.py tests/test_generate_synthetic.py`
      exits 0 with no warnings; `uv run ty check src/generate_synthetic.py` exits 0
  </acceptance_criteria>
  <done>Corpus + sampling + distribution functions implemented and passing dedicated unit tests with RED→GREEN commit history; lint/type clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED+GREEN — coverage validation (check_coverage)</name>
  <files>src/generate_synthetic.py, tests/test_generate_synthetic.py</files>
  <read_first>
    - src/generate_synthetic.py (current state from Task 1 — extend, do not rewrite)
    - .planning/phases/06-synthetic-generation/06-RESEARCH.md ("Coverage Validation" code block, Open Question 3)
    - .planning/phases/06-synthetic-generation/06-PATTERNS.md (Error handling / early-exit pattern, lines 116-147)
  </read_first>
  <behavior>
    - test_check_coverage_returns_chars_below_threshold: labels `["אאא", "ב"]`,
      `min_char_count=2` → returns dict `{"ב": 1}` (only sub-threshold chars; `"א"`
      at count 3 excluded).
    - test_check_coverage_nfc_normalizes_before_counting: a decomposed-form label and
      its NFC-composed equivalent count as the same character (no double-count from
      combining marks).
    - test_check_coverage_empty_when_all_meet_threshold: all chars at/above
      `min_char_count` → returns `{}`.
    - test_check_coverage_before_after_gap_shrinks: gaps computed on
      `existing_labels` vs `existing_labels + synthetic_labels` — adding synthetic
      labels with the rare char removes it from the returned gap dict.
  </behavior>
  <action>
    Add `check_coverage(labels: list[str], min_char_count: int) -> dict[str, int]`
    to `src/generate_synthetic.py`: build a `Counter` over
    `unicodedata.normalize("NFC", lbl)` for every label; return
    `{ch: cnt for ch, cnt in counter.items() if cnt < min_char_count}`. Pure
    function — no printing, no sys.exit (the caller in Plan 03 owns the exit-code
    decision per 06-PATTERNS.md Error handling pattern).

    Write the four `<behavior>` tests first, confirm RED, implement to GREEN. Keep
    lint/type clean. Commit as `test(06-02): ...` then `feat(06-02): ...`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_generate_synthetic.py -x -q && uv run ruff check src/generate_synthetic.py tests/test_generate_synthetic.py && uv run ty check src/generate_synthetic.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/generate_synthetic.py` contains
      `def check_coverage(labels: list[str], min_char_count: int) -> dict[str, int]:`
    - `check_coverage` contains NO `sys.exit` and NO `print(` call
      (grep within the function body returns nothing)
    - `tests/test_generate_synthetic.py` contains the 4 named coverage test functions;
      `uv run pytest tests/test_generate_synthetic.py` exits 0
    - `git log --oneline -2` shows a `test(06-02):` commit then a `feat(06-02):`
      commit for the coverage function
    - `uv run ruff check` and `uv run ty check src/generate_synthetic.py` exit 0
  </acceptance_criteria>
  <done>check_coverage implemented as a pure, NFC-normalizing, side-effect-free function with passing RED→GREEN tests; the exit-code decision is deferred to Plan 03's main().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing manifest.csv → corpus builder | Label text (personal Hebrew notes) read and split; may contain noise (digits, Latin, punctuation) |
| --wordlist file → corpus builder | Optional user-supplied word file merged into the corpus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | noisy/mixed-script manifest labels polluting corpus | mitigate | Hebrew-only filter (U+05D0–U+05EA) drops non-Hebrew words before they enter the pool (RESEARCH.md Pitfall 5); covered by test_build_word_corpus_filters_non_hebrew_words |
| T-06-05 | Information Disclosure | personal Hebrew note text held in memory during corpus build | accept | Data stays local (PROJECT.md constraint); no network in this plan; no logging of raw label content |
| T-06-06 | Denial of Service | empty/all-noise manifest causing silent empty corpus | mitigate | build_word_corpus raises ValueError when no Hebrew words remain; test_build_word_corpus_empty_labels_raises asserts it |
</threat_model>

<verification>
- `uv run pytest tests/test_generate_synthetic.py` passes (all 11 test functions)
- Four pure functions present with exact signatures; no trdg/PIL module-level import
- RED→GREEN commit pairs visible in git log
- ruff + ty clean on the module
</verification>

<success_criteria>
The deterministic core of synthetic generation — corpus weighting (SYN-03), empirical
length sampling (D-05), and coverage gap detection (SYN-04) — is implemented as tested pure
functions that Plan 03 can wire into a rendering loop with confidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-synthetic-generation/06-02-SUMMARY.md`
</output>
