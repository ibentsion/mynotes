---
phase: 06-synthetic-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - assets/fonts/.gitkeep
autonomous: true
requirements: [SYN-01]
must_haves:
  truths:
    - "uv sync installs trdg 1.8.0 without downgrading numpy below 2.4.4"
    - "from trdg.generators.from_strings import GeneratorFromStrings succeeds in the venv"
    - "assets/fonts/ exists and its .ttf contents are gitignored"
  artifacts:
    - path: "pyproject.toml"
      provides: "trdg + transitive deps + override-dependencies + console script"
      contains: "trdg==1.8.0"
    - path: "assets/fonts/.gitkeep"
      provides: "tracked empty fonts directory"
    - path: ".gitignore"
      provides: "assets/fonts/*.ttf ignore rule"
      contains: "assets/fonts"
  key_links:
    - from: "pyproject.toml [tool.uv] override-dependencies"
      to: "uv resolver"
      via: "arabic-reshaper / python-bidi / opencv-python-headless pins"
      pattern: "override-dependencies"
---

<objective>
Add the TRDG dependency stack to the project and prove it installs without breaking the
existing numpy/torch/opencv environment. This is the foundation gate for Phase 6 — every
subsequent plan imports `trdg`.

Purpose: trdg 1.8.0's transitive pins (`arabic-reshaper==2.1.3` yanked, trdg's
`opencv-python` vs project's `opencv-python-headless`) silently downgrade numpy to 1.16.6
and break PyTorch. The `[tool.uv] override-dependencies` mechanism is the only safe install
path (RESEARCH.md Pitfall 1 + 6, verified live with `uv pip install --dry-run`).

Output: updated `pyproject.toml`, `.gitignore` rule for downloaded fonts, tracked
`assets/fonts/.gitkeep`, a verified `uv sync` with numpy intact, and the `generate-synthetic`
console script registered (entry point file created by Plan 03; registering it here keeps
all `pyproject.toml` edits in one plan to avoid cross-plan file contention).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-synthetic-generation/06-RESEARCH.md
@.planning/phases/06-synthetic-generation/06-PATTERNS.md

<interfaces>
Current pyproject.toml state (the file being modified):
- [project] dependencies: a list of `==`-pinned packages including
  `opencv-python-headless==4.13.0.92`, `numpy==2.4.4`, `torch==2.11.0`.
- [project.scripts]: existing entries follow `verb-noun = "src.module:main"`
  (e.g. `tune-hpo = "src.tune:main"`).
- [tool.uv]: currently only `package = true`. There is NO existing
  `override-dependencies` key.
- [tool.uv.sources] / [[tool.uv.index]]: torch is pinned to the pytorch-cpu index —
  these blocks MUST remain unchanged.

.gitignore already ignores `outputs/*` (with `!outputs/.gitkeep`) and `data/`.
There is NO existing `assets/` rule. There is no `assets/` directory yet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TRDG dependency stack and override-dependencies to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (entire file — current dependency list, [tool.uv] block, [project.scripts] block)
    - .planning/phases/06-synthetic-generation/06-RESEARCH.md (Pitfall 1, Pitfall 2, Pitfall 6, Standard Stack table)
    - .planning/phases/06-synthetic-generation/06-PATTERNS.md (pyproject.toml section, lines 231-266)
  </read_first>
  <action>
    Edit pyproject.toml making exactly these changes:

    1. In `[project].dependencies`, append three new entries after `"matplotlib>=3.9",`:
       `"trdg==1.8.0"`, `"arabic-reshaper==3.0.1"`, `"python-bidi==0.6.10"`.
       Use exact `==` pins (project convention — no `^`/`~`/`>=` for new pins;
       matplotlib's existing `>=3.9` stays as-is, do not touch other lines).

    2. In the existing `[tool.uv]` table, add a new key `override-dependencies` as a
       TOML array of three strings, immediately after the existing `package = true`
       line (keep `package = true`):
       `override-dependencies = ["arabic-reshaper==3.0.1", "python-bidi==0.6.10", "opencv-python-headless==4.13.0.92"]`
       Rationale to preserve as a comment above the array: trdg pins
       `arabic-reshaper==2.1.3` (yanked) and `opencv-python` (conflicts with the
       project's `opencv-python-headless`); the override forces the resolver to the
       working versions (RESEARCH.md Pitfall 1 + 6).

    3. In `[project.scripts]`, add the line
       `generate-synthetic = "src.generate_synthetic:main"` following the existing
       `verb-noun = "src.module:main"` convention (place it after
       `tune-hpo = "src.tune:main"`). The target module is created in Plan 03.

    Do NOT modify `[tool.uv.sources]`, `[[tool.uv.index]]`, `[build-system]`,
    `[tool.hatch.build.targets.wheel]`, `[tool.ruff]`, or `[tool.pytest.ini_options]`.
  </action>
  <verify>
    <automated>grep -q 'trdg==1.8.0' pyproject.toml && grep -q 'override-dependencies' pyproject.toml && grep -q 'generate-synthetic = "src.generate_synthetic:main"' pyproject.toml && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'trdg==1.8.0' in d['project']['dependencies']; assert 'arabic-reshaper==3.0.1' in d['tool']['uv']['override-dependencies']; assert d['project']['scripts']['generate-synthetic']=='src.generate_synthetic:main'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[project].dependencies` contains `trdg==1.8.0`,
      `arabic-reshaper==3.0.1`, `python-bidi==0.6.10`
    - `pyproject.toml` `[tool.uv].override-dependencies` is a list equal to
      `["arabic-reshaper==3.0.1", "python-bidi==0.6.10", "opencv-python-headless==4.13.0.92"]`
    - `pyproject.toml` `[project.scripts].generate-synthetic` equals
      `"src.generate_synthetic:main"`
    - `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0
      (file is still valid TOML)
    - `[tool.uv.sources]` and `[[tool.uv.index]]` blocks are byte-identical to before
      (`git diff pyproject.toml` shows no changes inside those blocks)
  </acceptance_criteria>
  <done>pyproject.toml declares the TRDG stack with overrides and the console script; file parses as valid TOML; torch/pytorch-cpu index config untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Gitignore downloaded fonts, track assets/fonts/, and verify uv sync keeps numpy intact</name>
  <files>.gitignore, assets/fonts/.gitkeep</files>
  <read_first>
    - .gitignore (entire file — existing structure, the outputs/ + data/ rules)
    - .planning/phases/06-synthetic-generation/06-RESEARCH.md (Pitfall 1 "Warning signs", Open Question 1)
  </read_first>
  <action>
    1. Create the directory `assets/fonts/` and create an empty file
       `assets/fonts/.gitkeep` (tracked, so the directory exists before first font
       download in Plan 03).

    2. In `.gitignore`, add a new section after the existing "Pipeline outputs"
       block (do not reorder or remove existing rules):
       a header comment line `# Downloaded fonts (binary, OFL — name list lives in source)`
       followed by `assets/fonts/*.ttf` on its own line and `!assets/fonts/.gitkeep`
       on the next line so the .gitkeep stays tracked while .ttf files are ignored.

    3. Run `uv sync` and capture the output. Then verify the environment is intact:
       run `uv run python -c "import numpy; print(numpy.__version__)"` and confirm it
       prints `2.4.4` (NOT 1.16.x — that would mean the override failed, RESEARCH.md
       Pitfall 1 warning sign). Run
       `uv run python -c "from trdg.generators.from_strings import GeneratorFromStrings; print('trdg-ok')"`
       and confirm it prints `trdg-ok` with no `wikipedia` ImportError (RESEARCH.md
       Pattern 1 — must use the `.from_strings` submodule path). Run
       `uv run python -c "import torch, cv2; print(torch.__version__, cv2.__version__)"`
       and confirm both import without error.

    If `numpy` is not `2.4.4` after `uv sync`, STOP and report — the
    override-dependencies in Task 1 are misconfigured; do not proceed.
  </action>
  <verify>
    <automated>test -f assets/fonts/.gitkeep && grep -q 'assets/fonts/\*.ttf' .gitignore && grep -q '!assets/fonts/.gitkeep' .gitignore && uv sync >/dev/null 2>&1 && [ "$(uv run python -c 'import numpy;print(numpy.__version__)')" = "2.4.4" ] && uv run python -c "from trdg.generators.from_strings import GeneratorFromStrings" && uv run python -c "import torch, cv2"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/fonts/.gitkeep` exists and is tracked by git
      (`git status --porcelain assets/fonts/.gitkeep` shows it staged/tracked, not ignored)
    - `.gitignore` contains the literal lines `assets/fonts/*.ttf` and
      `!assets/fonts/.gitkeep`
    - `uv run python -c "import numpy; print(numpy.__version__)"` outputs exactly `2.4.4`
    - `uv run python -c "from trdg.generators.from_strings import GeneratorFromStrings"`
      exits 0 with no traceback (no transitive `wikipedia` import error)
    - `uv run python -c "import torch, cv2"` exits 0
    - `uv.lock` updated and contains a `trdg` entry at version `1.8.0`
  </acceptance_criteria>
  <done>Fonts directory tracked via .gitkeep, .ttf binaries gitignored, uv sync succeeds with numpy 2.4.4 and importable trdg/torch/cv2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local venv | Third-party packages (trdg, transitive deps) installed into the environment |
| Resolver overrides → dependency graph | `override-dependencies` forces non-default versions, bypassing trdg's declared pins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | trdg/transitive packages from PyPI | mitigate | All deps pinned with exact `==` versions (project supply-chain convention); `uv.lock` committed so installs are reproducible and hash-checked by uv |
| T-06-02 | Denial of Service | numpy downgrade breaking torch/cv2 | mitigate | `override-dependencies` pins arabic-reshaper/python-bidi/opencv-headless; Task 2 explicitly asserts numpy==2.4.4 and torch/cv2 import before proceeding |
| T-06-03 | Elevation of Privilege | malicious transitive package postinstall | accept | trdg is unmaintained but widely used; pinned versions verified against PyPI JSON API in RESEARCH.md; no postinstall scripts in pure-Python wheels; risk low for local personal tool |
</threat_model>

<verification>
- `uv sync` completes with no resolver error and no numpy/torch downgrade
- `from trdg.generators.from_strings import GeneratorFromStrings` importable in venv
- `pyproject.toml` is valid TOML and registers the `generate-synthetic` script
- `assets/fonts/.gitkeep` tracked; `assets/fonts/*.ttf` ignored
</verification>

<success_criteria>
The TRDG dependency stack is installed and importable without degrading the existing
numpy/torch/opencv environment, and the project is structurally ready for Plans 02-03 to
implement `src/generate_synthetic.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-synthetic-generation/06-01-SUMMARY.md`
</output>
