---
phase: 05-hyperparameter-tuning-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/ctc_utils.py
  - tests/test_ctc_utils.py
autonomous: true
requirements:
  - HPO-01  # CRNN architecture parameterized (rnn_hidden, num_layers) — D-02 prereq
  - HPO-02  # optuna 4.8.0 dependency added (D-03 backend choice)

must_haves:
  truths:
    - "CRNN can be instantiated with arbitrary rnn_hidden ∈ {128, 256, 512} and num_layers ∈ {1, 2}"
    - "fc layer output size scales correctly with rnn_hidden (rnn_hidden * 2 because BiLSTM)"
    - "Default CRNN(num_classes=N) still produces an identical model to the pre-Phase-5 hardcoded one (rnn_hidden=256, num_layers=2)"
    - "optuna 4.8.0 is importable from the project venv"
  artifacts:
    - path: "src/ctc_utils.py"
      provides: "CRNN.__init__ with rnn_hidden and num_layers parameters"
      contains: "def __init__(self, num_classes: int, rnn_hidden: int = 256, num_layers: int = 2)"
    - path: "pyproject.toml"
      provides: "optuna 4.8.0 in [project] dependencies"
      contains: "optuna==4.8.0"
    - path: "tests/test_ctc_utils.py"
      provides: "Parameterized CRNN tests covering {128,256,512} x {1,2}"
      contains: "rnn_hidden"
  key_links:
    - from: "src/ctc_utils.py::CRNN.__init__"
      to: "self.rnn = nn.LSTM(... , rnn_hidden, num_layers=num_layers, ...)"
      via: "constructor argument plumbing"
      pattern: "nn\\.LSTM\\(128 \\* 8, rnn_hidden, num_layers=num_layers"
    - from: "src/ctc_utils.py::CRNN.__init__"
      to: "self.fc = nn.Linear(rnn_hidden * 2, num_classes)"
      via: "BiLSTM doubling — Pitfall 1"
      pattern: "nn\\.Linear\\(rnn_hidden \\* 2, num_classes\\)"
---

<objective>
Parameterize the CRNN architecture (`rnn_hidden`, `num_layers`) and add optuna 4.8.0 to the
project dependencies. This is a strict prerequisite (D-02) — the hyperparameter tuner cannot
vary architecture parameters that are hardcoded inside `CRNN.__init__`.

Purpose: Unblock Phase 5 plans 02 and 03 by giving them a parameterized model and an installed
search backend.
Output: Modified `src/ctc_utils.py` with parameterized CRNN, updated `pyproject.toml` + `uv.lock`
with optuna, and tests covering the architecture sweep space.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hyperparameter-tuning-system-research-optuna-vs-alternatives-set-up-tuning-infrastructure-with-batch-jobs-integrate-clearml-hpo-reporting-reusable-cli-entry-point-for-retuning/05-CONTEXT.md
@.planning/phases/05-hyperparameter-tuning-system-research-optuna-vs-alternatives-set-up-tuning-infrastructure-with-batch-jobs-integrate-clearml-hpo-reporting-reusable-cli-entry-point-for-retuning/05-RESEARCH.md
@src/ctc_utils.py
@tests/test_ctc_utils.py

<interfaces>
<!-- CRNN.__init__ before this plan (src/ctc_utils.py:302) -->
class CRNN(nn.Module):
    def __init__(self, num_classes: int) -> None:
        super().__init__()
        self.cnn = nn.Sequential(...)  # CNN output is fixed at 128*8 = 1024 regardless of rnn_hidden
        self.rnn = nn.LSTM(128 * 8, 256, num_layers=2, bidirectional=True, batch_first=False)
        self.fc = nn.Linear(512, num_classes)  # 512 = 256 * 2 (bidirectional)

<!-- CRNN.__init__ AFTER this plan -->
class CRNN(nn.Module):
    def __init__(
        self, num_classes: int, rnn_hidden: int = 256, num_layers: int = 2
    ) -> None:
        super().__init__()
        self.cnn = nn.Sequential(...)  # unchanged
        self.rnn = nn.LSTM(
            128 * 8, rnn_hidden, num_layers=num_layers, bidirectional=True, batch_first=False
        )
        self.fc = nn.Linear(rnn_hidden * 2, num_classes)  # bidirectional doubles

<!-- Default values MUST remain rnn_hidden=256, num_layers=2 so existing call sites
     (train_ctc.py uses CRNN(num_classes=len(charset) + 1)) continue working unchanged. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optuna 4.8.0 to project dependencies</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (current dependency list and ordering)
    - .planning/phases/05-hyperparameter-tuning-system-research-optuna-vs-alternatives-set-up-tuning-infrastructure-with-batch-jobs-integrate-clearml-hpo-reporting-reusable-cli-entry-point-for-retuning/05-RESEARCH.md (Standard Stack section — confirms optuna==4.8.0)
  </read_first>
  <action>
    Add `"optuna==4.8.0"` to the `[project] dependencies` list in pyproject.toml.
    Insert it as a new line keeping the existing pinned-version style (`==`), after the
    `"openai>=1.0"` line. Do NOT change other dependencies. Do NOT add it to a dependency-group.

    Then run `uv sync` to update `uv.lock` and install optuna into the venv:
      uv sync

    Verify the install with `uv pip show optuna` — must report `Version: 4.8.0`.

    Notes:
    - Per project CLAUDE.md: pin exact versions (`==`), justify dependencies. Justification:
      Optuna is the chosen HPO backend (RESEARCH.md "Backend Decision" — verdict: Optuna
      standalone over ClearML HyperParameterOptimizer).
    - Do NOT touch `[tool.uv.sources]` — optuna is published normally on PyPI, not via the
      pytorch-cpu index.
  </action>
  <verify>
    <automated>grep -E '^\s*"optuna==4\.8\.0",?\s*$' pyproject.toml &amp;&amp; uv pip show optuna | grep -E '^Version: 4\.8\.0$' &amp;&amp; python -c "import optuna; assert optuna.__version__ == '4.8.0'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"optuna==4.8.0"' pyproject.toml` returns a match
    - `uv pip show optuna` reports `Version: 4.8.0`
    - `python -c "import optuna; print(optuna.__version__)"` prints `4.8.0`
    - `uv.lock` contains an `[[package]]` entry with `name = "optuna"` and `version = "4.8.0"`
    - No other dependency lines were modified (run `git diff pyproject.toml` and confirm only the optuna line was added)
  </acceptance_criteria>
  <done>optuna 4.8.0 is installed, pinned in pyproject.toml, locked in uv.lock, and importable from the project venv.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parameterize CRNN with rnn_hidden and num_layers</name>
  <files>src/ctc_utils.py, tests/test_ctc_utils.py</files>
  <read_first>
    - src/ctc_utils.py (CRNN class at line 293 — current hardcoded constants)
    - tests/test_ctc_utils.py (existing CRNN tests at test_crnn_instantiates_without_error, test_crnn_forward_output_shape, test_crnn_forward_returns_raw_logits_no_softmax — pattern to follow)
    - .planning/phases/05-hyperparameter-tuning-system-research-optuna-vs-alternatives-set-up-tuning-infrastructure-with-batch-jobs-integrate-clearml-hpo-reporting-reusable-cli-entry-point-for-retuning/05-RESEARCH.md (Pitfall 1: fc layer must be rnn_hidden * 2)
  </read_first>
  <behavior>
    - Test 1: `CRNN(num_classes=10)` (no kwargs) instantiates without error and has `self.rnn.hidden_size == 256`, `self.rnn.num_layers == 2`, `self.fc.in_features == 512`
    - Test 2: `CRNN(num_classes=10, rnn_hidden=128, num_layers=1)` produces `self.rnn.hidden_size == 128`, `self.rnn.num_layers == 1`, `self.fc.in_features == 256`
    - Test 3: `CRNN(num_classes=10, rnn_hidden=512, num_layers=2)` produces `self.rnn.hidden_size == 512`, `self.fc.in_features == 1024`
    - Test 4: forward pass with `rnn_hidden=128` on input shape `(2, 1, 64, 128)` returns shape `(32, 2, num_classes)` — i.e. width // 4 timesteps preserved regardless of rnn_hidden
    - Test 5: forward pass with `num_layers=1` on input shape `(2, 1, 64, 128)` runs without error and returns the same shape `(32, 2, num_classes)`
  </behavior>
  <action>
    Modify `src/ctc_utils.py::CRNN.__init__` from:
      def __init__(self, num_classes: int) -> None:
          super().__init__()
          self.cnn = nn.Sequential(...)  # unchanged
          self.rnn = nn.LSTM(128 * 8, 256, num_layers=2, bidirectional=True, batch_first=False)
          self.fc = nn.Linear(512, num_classes)
    to:
      def __init__(
          self, num_classes: int, rnn_hidden: int = 256, num_layers: int = 2
      ) -> None:
          super().__init__()
          self.cnn = nn.Sequential(...)  # unchanged
          self.rnn = nn.LSTM(
              128 * 8,
              rnn_hidden,
              num_layers=num_layers,
              bidirectional=True,
              batch_first=False,
          )
          self.fc = nn.Linear(rnn_hidden * 2, num_classes)

    The CNN block is UNCHANGED (CNN output is fixed at 128 * 8 = 1024 regardless of rnn_hidden —
    confirmed in RESEARCH.md Pattern 2).

    Defaults MUST remain `rnn_hidden=256, num_layers=2` so existing call sites in
    `src/train_ctc.py` (`CRNN(num_classes=len(charset) + 1)`) continue producing the identical
    model architecture as before this plan. NO changes to `forward()`. NO changes to any other
    function in ctc_utils.py.

    Then add 5 new pytest tests to `tests/test_ctc_utils.py` in the existing CRNN test section
    (after `test_crnn_forward_returns_raw_logits_no_softmax` at line 261). Use plain function
    test pattern matching the existing style:

      def test_crnn_default_kwargs_match_legacy_architecture():
          model = CRNN(num_classes=10)
          assert model.rnn.hidden_size == 256
          assert model.rnn.num_layers == 2
          assert model.fc.in_features == 512

      def test_crnn_accepts_smaller_rnn_hidden_and_single_layer():
          model = CRNN(num_classes=10, rnn_hidden=128, num_layers=1)
          assert model.rnn.hidden_size == 128
          assert model.rnn.num_layers == 1
          assert model.fc.in_features == 256

      def test_crnn_accepts_larger_rnn_hidden():
          model = CRNN(num_classes=10, rnn_hidden=512, num_layers=2)
          assert model.rnn.hidden_size == 512
          assert model.fc.in_features == 1024

      def test_crnn_forward_works_with_smaller_rnn_hidden():
          model = CRNN(num_classes=10, rnn_hidden=128, num_layers=2)
          x = torch.zeros(2, 1, 64, 128)  # batch=2, height=64, width=128
          out = model(x)
          assert out.shape == (32, 2, 10)  # T = W // 4 = 32

      def test_crnn_forward_works_with_single_layer():
          model = CRNN(num_classes=10, rnn_hidden=256, num_layers=1)
          x = torch.zeros(2, 1, 64, 128)
          out = model(x)
          assert out.shape == (32, 2, 10)

    Per project CLAUDE.md: no docstrings (CRNN init's existing docstring stays, but new tests get
    no docstrings). Run `uv run ruff check src/ctc_utils.py tests/test_ctc_utils.py` and
    `uv run ruff format src/ctc_utils.py tests/test_ctc_utils.py` after editing — fix any warnings.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ctc_utils.py -k "crnn" -v &amp;&amp; uv run ruff check src/ctc_utils.py tests/test_ctc_utils.py &amp;&amp; uv run ty check src/ctc_utils.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def __init__(self, num_classes: int, rnn_hidden: int = 256, num_layers: int = 2)" src/ctc_utils.py` returns a match
    - `grep -n "nn.LSTM(\s*128 \* 8,\s*rnn_hidden," src/ctc_utils.py` returns a match (multiline acceptable — `rg "rnn_hidden,\s*num_layers=num_layers" src/ctc_utils.py` also accepted)
    - `grep -n "nn.Linear(rnn_hidden \* 2, num_classes)" src/ctc_utils.py` returns a match
    - `grep -c "def test_crnn_default_kwargs_match_legacy_architecture\|def test_crnn_accepts_smaller_rnn_hidden_and_single_layer\|def test_crnn_accepts_larger_rnn_hidden\|def test_crnn_forward_works_with_smaller_rnn_hidden\|def test_crnn_forward_works_with_single_layer" tests/test_ctc_utils.py` returns 5
    - `uv run pytest tests/test_ctc_utils.py -k "crnn" -v` exits 0 with all CRNN tests (existing + 5 new) passing
    - `uv run ruff check src/ctc_utils.py tests/test_ctc_utils.py` exits 0 with no issues
    - `uv run ty check src/ctc_utils.py` reports no new type errors
    - Existing test `test_crnn_instantiates_without_error` still passes (proves backward-compat default)
  </acceptance_criteria>
  <done>CRNN.__init__ accepts (num_classes, rnn_hidden=256, num_layers=2). Default-arg construction is byte-identical to the pre-plan architecture. 5 new tests cover {128,256,512} hidden × {1,2} layers and forward-pass correctness on width 128.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_ctc_utils.py -v` exits 0 (all existing + 5 new tests pass)
- `uv run pytest tests/test_train_ctc.py -v` exits 0 (no regression — `CRNN(num_classes=...)` default call still works)
- `uv run ruff check .` exits 0
- `uv run ty check src/ctc_utils.py` reports no new errors
- `python -c "from src.ctc_utils import CRNN; m = CRNN(10, rnn_hidden=512, num_layers=1); print(m.rnn.hidden_size, m.fc.in_features)"` prints `512 1024`
- `python -c "import optuna; print(optuna.__version__)"` prints `4.8.0`
</verification>

<success_criteria>
- optuna 4.8.0 installed and importable from the project venv
- `pyproject.toml` and `uv.lock` updated with optuna pinned
- CRNN supports `rnn_hidden ∈ {128, 256, 512}` and `num_layers ∈ {1, 2}` via constructor kwargs
- Default-arg construction (`CRNN(num_classes=N)`) is identical to the pre-plan hardcoded model
  — required so train_ctc.py keeps working unchanged after this plan and before plan 02 lands
- All existing tests still pass; 5 new architecture-sweep tests added and pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-hyperparameter-tuning-system-research-optuna-vs-alternatives-set-up-tuning-infrastructure-with-batch-jobs-integrate-clearml-hpo-reporting-reusable-cli-entry-point-for-retuning/05-01-SUMMARY.md`
</output>
